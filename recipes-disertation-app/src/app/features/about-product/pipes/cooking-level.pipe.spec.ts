import { CookingLevelPipe } from './cooking-level.pipe';

describe('CookingLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new CookingLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
