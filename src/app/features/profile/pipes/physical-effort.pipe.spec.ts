import { PhysicalEffortPipe } from './physical-effort.pipe';

describe('PhysicalEffortPipe', () => {
  it('create an instance', () => {
    const pipe = new PhysicalEffortPipe();
    expect(pipe).toBeTruthy();
  });
});
